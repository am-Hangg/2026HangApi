import os from "os";

console.log(os.totalmem()); // kati rem ko vanera check garda (or size)

console.log(os.freemem()); // space tha pauna lai

console.log(os.cpus()); // katiko cpu x vanera che garne

console.log(os.platform()); // operating system check garne

console.log(os.version()); // version check garda